# .env
# Ambiente ativo
VITE_ENV=development

# Backend em desenvolvimento (usa proxy do Vite: /api -> 8080)
VITE_API_URL=/api

# -------- Produção (descomente e ajuste no deploy) --------
# VITE_ENV=production
# VITE_API_URL=https://SEU-BACKEND.AQUI
# ----------------------------------------------------------

# Observação: em produção, prefira definir estas variáveis no ambiente do servidor
# (Docker/K8s/CI) em vez de commitar alterado.
